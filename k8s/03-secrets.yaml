apiVersion: v1
kind: Secret
metadata:
  name: rag-app-secrets
  namespace: rag-ops-prod
type: Opaque
stringData:
  # In real life, use ExternalSecrets or SealedSecrets
  OPENAI_API_KEY: "insert-your-key-here"